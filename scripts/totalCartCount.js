

// Create a new data structure
// update the state (add, remove, filter)
// make an array of objects (cart items)
// use the length property on the array
// array is adjusted and you only have to update the view of state